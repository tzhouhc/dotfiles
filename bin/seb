#!/usr/bin/env zsh
# custom version of the `o` letter command to be used as a binary.
PATH=$PATH:$HOME/.cargo/bin:$HOME/.fzf/bin:$HOME/.dotfiles/bin

binding='enter:become(open {}),ctrl-o:become(open -R {})'
result=$( ( fd . ~/ --hidden & fd . "/Applications/" --extension app ) | fzf --bind "$binding" --preview 'smart_preview {}')

if [[ "$result" != '' ]]; then
  open $result
fi
