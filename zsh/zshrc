#!/usr/bin/env zsh

ZSH_HOME="$HOME/.zsh"

# common imports for both shell and other invocations of zsh
source "$ZSH_HOME/base.zsh"

# other configs and operations unique to login shells
source "$ZSH_HOME/shell.zsh"
